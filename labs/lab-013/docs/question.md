# Question

Solve this question on: `app-srv1`

1. Create user `contractor7` using the system's normal defaults — do not override the shell or home base path, and confirm what those defaults actually are before assuming them. Force `contractor7` to set their own password on next login (they should not start with a usable password already set). Set the account to fully expire (not just the password) in 30 days.
2. An existing account `contractor3` is under investigation and needs to be locked out immediately, without deleting the account or losing its configuration.
3. A fully offboarded account `contractor1` needs to be removed entirely, including its home directory.
