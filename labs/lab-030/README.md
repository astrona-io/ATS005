# Lab 030: Shell Environment & PATH Capstone

QEMU VM for the LFCS course — configuring a system-wide login-shell variable via `/etc/profile.d/`, a personal profile variable for one user, and a safely-appended personal PATH extension, all on one host.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS005.git -c labs/lab-030
```
