# lab-020: Access Control & Resource Limits Capstone

QEMU VM for the LFCS course — granting scoped, inheriting ACL access to two external users on a shared directory, replacing a sloppy per-user resource-limit hack with a proper PAM hard limit, and enforcing a group-wide maxlogins cap.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS005.git -c labs/lab-020
```
