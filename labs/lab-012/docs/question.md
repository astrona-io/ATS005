# Question

Solve this question on: `data-001`

1. Create group `datateam` with GID `5000` exactly. Add existing users `marta` and `cilla` to it as a supplementary group, without disturbing either user's existing primary group or other supplementary memberships.
2. Rename group `legacy-ops` (GID `4200`) to `platform-ops` without changing its GID or losing its membership list.
3. Delete the unused group `temp-audit` entirely.
