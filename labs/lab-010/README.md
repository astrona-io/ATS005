# lab-010: Local Account & Group Lifecycle Capstone

QEMU VM for the LFCS course — relocating an account's identity, provisioning a new hire with scoped sudo, running a full group lifecycle (create/rename/delete), and closing out the account lifecycle (lock, force-expire, remove).

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS005.git -c labs/lab-010
```
